{
  "name": "portal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma migrate deploy && next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest run",
    "pruefung": "tsc --noEmit && eslint && vitest run",
    "db:seed": "tsx prisma/seed.ts",
    "postinstall": "prisma generate"
  },
  "PLATZHALTER": true
}
